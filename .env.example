# ReelyRated Environment Variables
# Copy this file to .env and fill in your values

# ============================================================================
# REQUIRED: Supabase Configuration
# ============================================================================
# Get these from: https://app.supabase.com → Project Settings → API
#
# Your Supabase project URL (must be a valid Supabase URL)
VITE_SUPABASE_URL=https://your-project-id.supabase.co

# Your Supabase anon/public key (starts with 'eyJ')
# This is safe to expose in the client - it's the public key
VITE_SUPABASE_PUBLISHABLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# ============================================================================
# OPTIONAL: Admin Configuration
# ============================================================================
# Comma-separated list of admin user UUIDs
# These users will have access to the admin panel
# Leave empty if you don't have any admins yet
#
# Example: VITE_ADMIN_USER_IDS=uuid-1,uuid-2,uuid-3
VITE_ADMIN_USER_IDS=

# ============================================================================
# OPTIONAL: Public Site URL
# ============================================================================
# Full URL of your production site (for social sharing, OpenGraph, etc.)
# Only needed in production
#
# Example: VITE_PUBLIC_SITE_URL=https://reelyrated.com
VITE_PUBLIC_SITE_URL=

# ============================================================================
# OPTIONAL: OAuth Redirect URL
# ============================================================================
# Base URL for OAuth redirects (Google sign-in)
# Defaults to window.location.origin if not set
#
# Development: http://localhost:8080
# Production: https://reelyrated.com
VITE_APP_URL=

# ============================================================================
# Setup Instructions
# ============================================================================
#
# 1. Copy this file to .env:
#    cp .env.example .env
#
# 2. Get Supabase credentials:
#    - Go to https://app.supabase.com
#    - Select your project
#    - Go to Project Settings → API
#    - Copy the Project URL → VITE_SUPABASE_URL
#    - Copy the anon/public key → VITE_SUPABASE_PUBLISHABLE_KEY
#
# 3. (Optional) Add admin users:
#    - Create user accounts in your app
#    - Get the user UUIDs from Supabase Dashboard → Authentication → Users
#    - Add comma-separated UUIDs to VITE_ADMIN_USER_IDS
#
# 4. Start development server:
#    npm install
#    npm run dev
#
# ============================================================================
# Security Notes
# ============================================================================
#
# - Never commit .env to git (it's in .gitignore)
# - VITE_SUPABASE_PUBLISHABLE_KEY is safe to expose in client code
# - For production, set these in your hosting platform's env vars:
#   - Vercel: Project Settings → Environment Variables
#   - Netlify: Site Settings → Build & deploy → Environment
#
# - The VITE_ prefix is required for Vite to expose vars to the client
# - Variables without VITE_ prefix are NOT available in browser code
#
# ============================================================================
